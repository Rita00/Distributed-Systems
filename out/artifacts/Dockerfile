FROM openjdk:15
WORKDIR /
ADD rmiserver.jar rmiserver.jar
ADD server.jar server.jar
ADD console.jar console.jar
ADD terminal.jar terminal.jar
ADD config.properties config.properties
ADD Election.db Election.db
EXPOSE 8080
